import { Inter } from "next/font/google";
import { Poppins } from "next/font/google";

export const inter = Inter({weight:['800'],subsets: ['latin']});
export const poppins = Poppins({weight:['200'],subsets: ['latin']})
